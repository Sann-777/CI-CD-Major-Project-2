{
  "name": "studynotion-media-service",
  "version": "1.0.0",
  "description": "Media upload and management microservice for StudyNotion platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Build completed for media-service'",
    "test": "jest --passWithNoTests || exit 0",
    "test:watch": "jest --watch --passWithNoTests || exit 0",
    "test:coverage": "jest --coverage --passWithNoTests || exit 0",
    "test:ci": "jest --ci --coverage --passWithNoTests --silent || exit 0",
    "lint": "eslint . --ext .js,.json --fix || exit 0",
    "lint:check": "eslint . --ext .js,.json || exit 0",
    "format": "prettier --write '**/*.{js,json,md}' || exit 0",
    "format:check": "prettier --check '**/*.{js,json,md}' || exit 0",
    "clean": "rimraf node_modules coverage .nyc_output logs tmp",
    "validate": "npm run lint:check && npm run format:check && npm run test:ci",
    "security:audit": "npm audit --audit-level moderate || exit 0",
    "security:fix": "npm audit fix || exit 0",
    "prestart": "npm run validate || exit 0",
    "postinstall": "echo 'Media service dependencies installed'"
  },
  "dependencies": {
    "cloudinary": "^1.41.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-fileupload": "^1.5.2",
    "express-rate-limit": "^6.7.0",
    "helmet": "^6.1.5",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "cross-env": "^7.0.3",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-security": "^2.1.0",
    "jest": "^29.7.0",
    "jest-environment-node": "^29.7.0",
    "mongodb-memory-server": "^10.2.0",
    "nodemon": "^3.0.2",
    "prettier": "^3.2.4",
    "rimraf": "^5.0.5",
    "supertest": "^6.3.4"
  },
  "keywords": [
    "media",
    "upload",
    "microservice",
    "studynotion"
  ],
  "author": "StudyNotion Team",
  "license": "MIT"
}
