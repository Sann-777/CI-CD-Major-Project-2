apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingress.yaml

# Add common labels to all resources
labels:
  - pairs:
      app: studynotion
      component: ingress

# Add common annotations to all resources
commonAnnotations:
  description: "StudyNotion - Ingress Configuration"
  version: "1.0.0"
