apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

# Add common labels to all resources
labels:
  - pairs:
      app: auth-service
      component: microservice

# Add common annotations to all resources
commonAnnotations:
  description: "StudyNotion - Auth Service"
  version: "1.0.0"
