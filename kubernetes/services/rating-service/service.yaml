apiVersion: v1
kind: Service
metadata:
  name: rating-service
  labels:
    app: rating-service
    component: microservice
spec:
  type: ClusterIP
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
    name: http
  selector:
    app: rating-service
